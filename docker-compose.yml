version: '3.7'
services:
  servicemanagement:
    image: pro05/superesb-local-servicemanagement:latest
    container_name: esb_servicemgmt
    network_mode: host
    logging:
      driver: syslog
      #options:
      #  syslog-address: "tcp://localhost:5067"
